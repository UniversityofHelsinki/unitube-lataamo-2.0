import React from 'react';
import { render } from '@testing-library/react';
import LandingPage from "./LandingPage";

it('renders', () => {
    render(
        <LandingPage>
        </LandingPage>
    );
});
