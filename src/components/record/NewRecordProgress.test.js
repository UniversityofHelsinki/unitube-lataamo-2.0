import React from 'react';
import { render } from '@testing-library/react';
import NewRecordProgress from './NewRecordProgress';

it('renders', () => {
  render(<NewRecordProgress progress={{}} />);
});
